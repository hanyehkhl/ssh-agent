# Each user provides their own key here — never commit a real .env file.
LLM_API_KEY=your-api-key-here
# Any OpenAI-compatible endpoint. Leave empty to use OpenAI itself.
LLM_BASE_URL=
LLM_MODEL=gpt-4o
