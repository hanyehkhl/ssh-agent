# NOTICE — منبع الهام و رعایت کپی‌رایت

این پروژه از ریپوی زیر الهام گرفته شده است:

- **پروژه:** [specialteam/ai_terminal](https://github.com/specialteam/ai_terminal)
- **نویسنده:** [specialteam](https://github.com/specialteam)
- **لایسنس اصلی:** MIT License

`ai_terminal` یک SSH client سبک با PyQt5 و Paramiko است که یک قابلیت اولیهٔ AI
دارد (چک‌باکس «AI Mode» که یک درخواست تک‌شات به GPT-3.5 می‌فرستد و یک دستور
تولید می‌کند). `ssh-agent` همان ایدهٔ پایه — SSH client گرافیکی با کمک هوش
مصنوعی — را می‌گیرد و آن را به یک **ایجنت واقعی چندمرحله‌ای** (tool-calling
loop به‌جای یک فراخوانی تک‌شات) با یک **موتور ایمنی صریح** ارتقا می‌دهد.

## چه چیزی از `ai_terminal` الهام گرفته شده (بازنویسی‌شده، نه کپی مستقیم)

- ایدهٔ کلی: یک SSH client با PyQt5 که یک لایهٔ AI کنارش دارد.
- الگوی اتصال SSH با **Paramiko** (`invoke_shell` روی یک کانکشن پایدار).
- فرمت لاگ سشن به‌صورت **JSONL** (`session_log.jsonl`) با timestamp UTC.
- الگوی به‌روزرسانی UI thread-safe با **PyQt signals** از یک worker thread
  جدا (در `ai_terminal` برای فراخوانی OpenAI، اینجا برای کل حلقهٔ ایجنت).

هیچ‌کدام از این‌ها کپی مستقیم کد نیستند — همه از صفر برای معماری این پروژه
(ابزارهای متعدد، policy engine، پرامپت فارسی) بازنویسی شده‌اند، اما ایده و
الگوی طراحی از `ai_terminal` گرفته شده و لازم است ذکر شود.

## چه چیزی کاملاً جدید است (در `ai_terminal` وجود ندارد)

- موتور ایمنی سه‌سطحی SAFE/CONFIRM/BLOCKED (`agent/policy.py`)
- حلقهٔ ایجنتیک چندمرحله‌ای با ابزارهای متعدد (`run_command`, `read_file`,
  `system_facts`, `tail_log`) به‌جای یک فراخوانی تک‌شات تولید دستور
  (`agent/loop.py`, `agent/tools.py`)
  و مدل واقعاً خروجی هر دستور را می‌خواند و بر اساس آن تصمیم می‌گیرد
- سازگاری با هر endpoint سازگار با OpenAI (از طریق `LLM_BASE_URL`) به‌جای
  وابستگی سخت به OpenAI
- پرامپت سیستم دوزبانه/فارسی و نرمال‌سازی ورودی فارسی (`agent/prompts.py`)
- کارت‌های نمایش هر tool call در UI (دستور، دلیل، سطح ایمنی، خروجی)

## رعایت لایسنس

طبق شرایط MIT License، این پروژه هم با MIT License منتشر می‌شود (فایل
`LICENSE`) و همین فایل NOTICE به‌عنوان اعلام صریح استفاده از ایده/الگوهای
پروژهٔ اصلی نگه داشته می‌شود. اگر بخشی از کد *واقعی* `ai_terminal` به‌صورت
مستقیم در این پروژه کپی شود، باید نوتیس کپی‌رایت اصلی آن هم در بالای همان
فایل حفظ شود — طبق سیاست فعلی، چنین کپی مستقیمی انجام نشده و همه‌چیز
بازنویسی شده است.
