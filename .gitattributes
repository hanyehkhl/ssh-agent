# Default: let Git normalize text line endings in the repository.
* text=auto

# These are executed inside Linux containers. Checking them out with CRLF
# makes the kernel fail to find the interpreter ("bad interpreter: no such
# file or directory"), so they must stay LF on every platform.
*.sh text eol=lf
tests/docker/Dockerfile text eol=lf
